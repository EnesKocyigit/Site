<%- include('blocks/header', {bot, user, path}) %>
  <div class="jumbotron">
    <div>
      <h1><%= bot.user.username %> Dashboard</h1>
      <h4><% if(bot.appInfo.description.length > 3) { %> <%= bot.appInfo.description %> <% } else { %>Yap?mc?lar Svolter Ve Frepture'dir.<% }%></h4>
      <p><a class="btn btn-primary" href="/commands" role="button">Komutlar »</a></p>
    </div>
  </div>
<% include ./blocks/footer %>